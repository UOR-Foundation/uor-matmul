# VERIFICATION

Which axis of `just vv` discharges which class of claim.

| `just` recipe | Enforces | ID classes |
| --- | --- | --- |
| `just fmt-check` | the diff is reviewable | --- |
| `just model` | R1, R8, R10, R11, R13, and R15; the ledger has no active suspension, so `audit-deferral` holds every workspace document and source file to the same rule | `CM-01`, and the absence of `CN-*` |
| `just lint` | clippy at `-D warnings`, including the unsafe-documentation lints | --- |
| `just test` | the whole suite | `CS-*`, `CT-*`, `CD-*`, `CB-*`, `CK-*`, `CX-*`, `CA-01`, `CA-05`, `CP-01`, `CU-02` .. `CU-05`, `CU-07`, `CU-11`, `CG-*` |
| `just purity` | R2 by scope-tracking the source, R3 and R13 by grep, `CU-01` by disassembly, and the live float-root call graph plus its Atlas operation census by `audit-uor-float` | `CU-01`, `CA-05`, `CU-11` |
| `just no-alloc` | R7, C1 | `CA-03` |
| `just bdd` | R9 and R4's behavioural half | `CM-02`, `CM-03` |
| `just checked` | every accumulator operation checked, no overflow | `CT-02` |
| `just cross` | the corpus digest off the host | `CA-02` |
| `just cortex-m-run` | the parity checks run on a Cortex-M target under qemu-system, not merely compiled | `CB-11` |
| `just scaling` | fitted exponents, reported never asserted | `CG-01` .. `CG-07` |
| `just census` | the static issue census names a bottleneck per kernel sequence | `CG-11` |
| `just symbol-bandwidth` | the symbol path's achieved bytes/second against the host's own STREAM number, reported never asserted, with byte-identity asserted inside the timed region | `CG-14` |
| `just bridge-sweep` | the retained historical workspace protocol over the default and compatibility spellings of one Atlas operation, reported never asserted, with byte identity asserted inside the timed region | `CG-15` |
| `just uor-float-sweep` | symmetric `f32`/`f64` Atlas latency, throughput, and logical caller-traffic intervals against the retained exact traversal, matrixmultiply, and faer, followed by integer and tropical controls; output poisoning, views, triples, offers, faer's C setup/copy, and complete byte checks stay outside each calibrated interval, which contains only repeated real production calls. The forced-candidate supplement interleaves calibrated batches and reports Student 95% intervals plus paired raw ratios to the public production selector; all figures are reported, never asserted | `CG-21` |
| `just symbol-tabulated` | the source-pinned 32-identity calibration and 14-identity holdout/control corpus; exact BigInt design ranks and essential coordinates; live q/hash, EntrySet, scalar-fracture, selector, timer, raw-output, and reconciliation audits with planted defects; then paired public forced block-one Atlas table versus public coded Atlas decline with setup and complete byte guards outside the compute-only timer. Every immutable round is emitted and confidence intervals are reported, never asserted. The nonignored companion under `just test` reconciles representative B1 and B3 replays against the live Census. The 2026-08-08 fit was rejected by its same-key H01/H02 holdout, so this recipe licenses no selector change | `CG-16` |
| `just strassen-sweep` | the sub-cubic recursion's achieved MACs/second against the cubic packed walk on the i32-exact lane, reported never asserted, with byte-identity asserted inside the timed region | `CG-12` |
| `just swar-sweep` | the i64x2 SWAR broadcast sequence's achieved MACs/second against the dot-with-extends sequence and the portable reference, on wasm32-wasip1 under wasmtime, reported never asserted, with byte-identity asserted inside the timed region | `CG-17` |
| `just features` | every optional feature compiles, and `kappa`'s tests run | --- |
| `just fuzz` | totality over unstructured input | `CT-01`, `CT-03`, `CK-06` |
| `just miri` | undefined behaviour in the crate that has the `unsafe`; `CU-07`, under `just test`, is what asserts it is pointed there | --- |

## The two halves of R2, and why both are needed

`audit-purity` reads the source and `CU-01` reads the emitted instructions.
Neither covers the other, and that was measured rather than assumed:

- `CU-01` sees only what was codegen'd. An uncalled `pub fn` is not in the rlib
  at all --- the symbol is absent from a 1.9 MB rlib and from every `.s` the gate
  reads --- so a float add sitting in one is invisible here, and codegen'd in a
  *downstream* build where these gates do not run.
- `audit-purity` cannot see what the optimizer emitted, and cannot type-check.
  It tracks which names carry floats: parameters and `let` bindings whose
  declared type mentions `f32` or `f64`, float literals, `as` casts, the elements
  of a float slice bound by a `for`, and aliasing chains between them.

Both are run by `just purity`, and both are on the falsifiable list below.

## Every gate is falsifiable

A gate nobody has seen fail is indistinguishable from a gate that cannot. Each
of these has been checked by planting the defect it exists to catch:

| Gate | Planted defect | Reported |
| --- | --- | --- |
| `check-constants` (R1) | a model numeral restated in a shipped crate | yes |
| `check-constants` (R1), Atlas short constants | `ATLAS_MODALITY` restated as the literal `3` instead of reading the private generated view; reported at the declaration. The control aliases `atlas::MODALITY as usize`; the scanner also rejects the `128` in `i128`, so a type name is not evidence of a restated value | yes |
| `check-model` (`CM-01`), private Atlas view | generated `SCOPE` changed from the model's 4 to 5 in `crates/uor-matmul-core/src/generated.rs`; reported that exact artifact as stale. `just model-write` restored it from the typed model | yes |
| `check-model` (`CM-01`), private kernel alignment | the model-owned cache-line value was changed independently to zero, the non-radix value 63, and `2^30` beyond Rust's maximum representation alignment; each mutation failed model validation. Clearing either the measured blocking record's R1 allowlist bit or its explanation failed independently. The positive mutation to 128 remained valid and changed both the private generated constant and its sole `repr(align)` literal together, while the committed 64-byte value generates one private payload wrapper and its inline borrowed-view operation | yes |
| the exact API lock, additions as well as removals | `pub fn api_equality_falsification_plant()` added under `uor_matmul_core::error`; `check-api` failed with `0 removed, 1 added` and named the declaration. After its removal the same gate reported 1240 declarations exactly equal to the committed surface, including the pre-existing lookup specifications, root re-export, float convenience entries, and crate-root `tile_fits` macro that the former scanner missed | yes |
| `CA-05`, runtime, address, and structural halves | a 4096-byte `Vec` allocated inside the observed float call; the counting allocator reported one allocation where zero is required. The carrier fixture planted inline `[i128; 24]` arrays first in `AtlasCarrier` and then in `AtlasBlocks`; both owned representations were rejected. In the hot-loop fixture, replacing the kernel-derived source-site expression with `129`, adding separate worst-case A and B code stores, restoring a redundant projection-flag array, duplicating finite modality beside the six-state quotient, replacing the L1-minus-workspace cell budget with `64`, allocating the maximum output frame instead of `CELL_CAP`, and deleting the irreducible one-cell dispatch arm all failed at the corresponding structural witness. Aliasing finite-negative with finite-positive failed the exact quotient audit, and the live boundary differential checked all six representatives. The live unit witness also compares exact caller and view pointers and bounds both view sizes, so a copied backing object or inline carrier cannot satisfy it. The public compatibility fixture independently removed `repr(transparent)` and made `Scaled64`'s tuple field private; both dirty declarations were rejected. Its live half compile-locks every concrete f32 `Lane`/`Tabulated` hook and the historical `[i32]`/`[i32]`/`[i128]` workspace entries, then preserves pointer and length and observes writes through both the direct `wrap_i64s_mut` view and the trait-mediated lane borrow | yes |
| `CU-11` | widening the group-one selector admitted a native multiply reducer, and replacing or severing the native nibble terminus exposed `i8_product`; both were rejected from the reachable lookup/add family. Fixing `i8_nibble_products` to row zero likewise failed the signed-octet-indexed safe-row audit. The short-offer stream fixture then replaced its retained-spec Atlas pair with `E::mac`, reselected a spec per product, and associated `Wide` as the float stream lane; every scalar escape failed. Severing the dense one-dot bridge, replacing it with `Self::mac`, and entering Atlas twice also failed. A depth-sized source store, a `shape.k` carrier, and a severed lazy source read failed the depth-independent workspace audit. Selection by declared rather than executed work, omission of the narrow family, representation-blind cell pricing, omission of the edge corner, per-call dot-kernel resolution, and a split no-std resolver all failed. A function-local `cfg(test)` closure was planted ahead of a live Atlas call; the scanner retained that call while excluding standalone test items, preventing vacuous source coverage. Premature tiled and one-dot placement, rescaling the raw radix diagonal, removing the one signed-place fracture, replacing its quotient with a shift, and changing its `i128::MAX + 1` radix all failed; unequal-panel truncation, unchecked indexing, and a skipped non-finite/implicit-zero join failed independently. Replacing the valuation quotient with a shift and the centered word remainder with a low-byte cast also failed. Removing the full-row dense presentation, omitting its kernel census, narrowing its derived row tile to one, flattening a short-page count, and restoring the per-cell full-row count all failed the persistent-stream audit. In the q-reachable dictionary/TableBuild/gather graph, a literal prefix, even radix, hash multiplication, shift/XOR/mask probing, portable mask addressing, bit-scan row grading, an alternate f32 table graph, missing retained-address parity, and a zero-initialized full-grade Horner walk were each rejected. The retained-clock gate separately failed when either result/map check or poison was omitted, when its verdict ceased to use the paired 95% interval, when it compared the candidate with itself, or when its immutable legacy arm changed. The clean live graph retains nonzero lookup, complete-add, bounded placement, and encode counts, so an empty graph cannot pass | yes |
| `CU-11`, grouped-native/hash closure | The shared AVX2 tile edge from `avx2_lookup_i8` through `avx2_lookup_i8_depth` to the canonical nibble row was severed in the live source fixture; all four declarations that share it failed, while the scalar-table `avx2_lookup_reduce_i8` control remained admitted through its distinct direct terminus arm. In the column dictionary, restoring the redundant initial length modulus and restoring the stale reduced-length model bound failed independently. The retained clock then failed when one arm was unbatched, when within-sample chunk alternation was removed, or when a result guard moved inside the timed interval, in addition to its poison, complete-map, immutable-comparator, and paired-upper-95% plants. The restored common-batch clock passed all four depths with 64 chunk-interleaved pairs and every upper endpoint below 1.0; `MEASUREMENT-LOG.md` records the host-scoped result | yes |
| `CK-19` | the Laurent-address remainder was changed from Euclidean `rem_euclid` to truncating `%`. `every_laurent_grade_has_an_unbounded_atlas_address_ck_19` failed on the first extreme negative grade because the resulting address could not be represented as the canonical nonnegative coordinate (`TryFromIntError(())` at the negative-overflow context). Restoring Euclidean division makes the full signed-grade corpus round-trip | yes |
| `CK-20` | the interaction block was omitted from reconstruction by adding the global block twice. `the_exact_atlas_carrier_resolves_and_p3_separates_ck_20` failed on its first load-bearing interaction coordinate (`left: 11`, `right: 24`), while the restored sum of all four projectors reconstructs the carrier exactly | yes |
| `CK-21` | the finite-escape predicate was widened from the two reserved tags to every nonzero padding word. The packed-kind test failed on the first historical noncanonical word (`i32::MIN`), reporting that its NaN sentinel no longer retained the exponent-based kind. The restored two-tag predicate passes the same corpus | yes |
| `CD-30` | the direct Atlas placement exponent was increased by one. The symmetric differential failed at the first nonzero `f32` boundary (`8.000001` instead of `4.0000005`) against the independently accumulated finite-NAF reference. Separately toggling the first packed result's IEEE code after the optimized call failed immediately, including at the empty-reduction boundary. WASI execution exposed a harness defect in the dishonest-range witness: its release panic aborts rather than unwinds. The production predicate is now tested without unwinding at both exact boundaries and immediately outside a dishonest singleton on every target; the public-call panic remains an additional host witness | yes |
| `CD-31` | the canonical minimum-gauge witness was changed to choose the greatest admissible right endpoint instead of the least. The exhaustive interval-family test failed on family `0x3`: the planted witness returned gauge 1 where the brute-force certificate required 2 and coverage no longer held. Restoring the least-right-endpoint choice agrees with the exhaustive optimum on every family | yes |
| `CD-32`, R9 total-q-carrier contract | Red-first produced three independent implementation witnesses: the lane reported the conservative `281474976710656` instead of the exact `281474943156225`; a capacity-one block-two forced call issued zero table reads and one dense kernel call; and the full binary32 exponent sweep declined its resident forced table. The completed q carrier now passes those same production boundaries: exact Q capacity and all 256 exponent fields plus all seven Complete unions stay table-resident with zero multiplies/kernel calls; a codec block wider than its one-product capacity scalar-fractures with exact Census `table_reads=8`, `adds=24`, `decodes=44`; `k=0` has the all-zero Census; const-parametric block/code spaces 3 and 5 preserve dense bytes across repeats, row/column tails, a non-unit source stride, and offers 0, 1, and complete; and finite/special/finite atoms preserve the complete accumulator in source order. The capacity and scalar-fracture witnesses read the generated model constants used by production rather than reopening repository files, so the same release tests execute under WASI and other filesystem-free targets; a planted target-time model reload is rejected by the structural audit. The extent-fractal kernel differential pins one lookup per occupied coordinate pair and one observation per occupied product grade, including `(1,1)` and `(16,7)` work endpoints. Model mutations narrowing the product, dropping a Complete state, moving the top-positive base, or shortening capacity each failed. Structural plants then removed each boundary/Census/kernel witness, self-compared or vacated value-blind route selection, severed the dynamic q seam, replaced the least per-slot L-infinity certificate or radix regrade, fractured every source eagerly, and introduced fixed fracture storage; every mutation was reported before the restored gate passed | yes |
| `CG-21`'s compute-only guarded measurement | public view construction and faer's C-to-caller adapter copy were each planted inside an `Instant` interval, faer's actual C state poison was omitted and then made fixed rather than expected-derived, the packed production call was replaced by an inert call, and the post-batch byte comparison was removed. A fixed public poison, length-blind and self-comparing byte guards, aggregate-only output, and discarded raw durations failed independently. The forced-candidate instrument was separately planted with in-timer view construction, an elided engine call, a missing post-timer byte check, a fixed poison, length-blind and self-comparing guards, unlabeled float width, and discarded raw durations. `float_sweep_times_only_real_production_calls_cg_21` rejected every mutation | yes |
| `CG-23`'s native changed/control protocol | red-first failed before the seven native cases had closed classes and a paired protocol. The completed gate classifies four emitted-code-changing cases and three normalized static controls, rejects relabelling a changed case as a control, and rejects giving either arm a different safe wrapper. Separate native plants replace the hidden product symbol's address-only `lea` with a memory-reading `mov`, change its hidden declaration to global, sever the canonical nibble/product terminus, duplicate a per-product alphabet access, omit the grouped tile half, and omit or duplicate the second MR1 reduction half. The restored preflight passed model generation/check, seven audit plants, live reachability, five exhaustive release parity tests, the one-payload/relocation inspection, and all four changed-case upper-95% rules; static controls emitted all 256 pairs without a timing assertion | yes |
| `CG-22` | the suggested integer scratch was replaced by an empty offer; result bytes remained valid but the execution ledger reported `ReferenceByOffer`, so the non-float control caught the regression. Reversing the AVX2 one-row lookup and established equal-height native M1 declarations then let the later lookup capture every positive-row ordinary i8 selection; the family-order audit rejected that mutation, while a separate chooser-boundary mutation proved that zero rows and positive rows cannot be conflated. Independently, removing, duplicating, or mismatching capacity arm 64, replacing the symbolic terminal with literal 128, and deriving a 129-lane kernel extent all failed the exact contiguous dispatch audit. The shipped/model storage-and-work differential then failed when the fixed workspace charge or live product-initialization coordinate was zeroed, when shipped cell residency invoked the model twin and compared it with itself, and when the `f64` comparison was removed. Restoring padded-tile product clearing, hiding the live-cell traversal invariant, and clearing a whole reused coordinate word instead of only its retired suffix each failed its source plant; the clean runtime census initializes exactly `m*n*k` product carriers and the independent binary oracle matches the quotient valuation over every `u16` plus all `u64` width boundaries. Cross-target execution then caught two real reachability defects: the NEON/wasm two-page shuffle did not project the complete octet alphabet, and the route witness required a native tile even on targets that expose none. The restored native projectors use one full-byte quotient/remainder recurrence, and the witness now equates native-tile observation with native-tile availability | yes |
| `audit-limits` (R8) | a `Result` over an unsanctioned error | yes |
| `audit-purity` (R2) | eight float-arithmetic plants: two typed params added, a `f64` multiply, a typed `let`, a `for` over `&[f32]`, `mul_add`, an `as f32` cast, two `*const f64` derefs, and a two-step aliasing chain | yes |
| `audit-disassembly` (`CU-01`) | a `black_box`-guarded float add inside an emitted function, reported as `addss` | yes |
| the `f64_exact` exemption, both directions | an emitted `fmul` inside a function holding the `F64Exact` witness: not reported. The same body without the marker: reported by both halves, as arithmetic on a float in the source and as `fmul` in the disassembly | yes |
| the `f64_exact` marker without the witness | a function named `*_f64_exact` that never mentions `F64Exact`: reported by the source half, which is the half that can see the type | yes |
| the `F64Exact` precondition | `F64Exact::<Bnd<{1 << 26}>, 3>::new()` --- 3 * 2^52 exceeds 2^53 --- fails to compile with the assertion's own message. Checked at monomorphization, so `cargo check` alone does not see it; a test build does | yes |
| `audit-disassembly` (`CU-06`) | a `black_box`-guarded value multiply in `gather_run`'s column loop, reported as `mul` in both `gather_reference_*` bodies. The Mach-O framing defect this exposed --- every body ran to end-of-file, so a dense NEON kernel's `sdot` was attributed to the gather --- is the one the gate actually shipped. AArch64 later supplied the opposite falsifier: LLVM lowered the permitted runtime remainder to adjacent `udiv`/`msub`, which the scanner initially called a value product. The gate now exempts only the exact operand-matched quotient identity; a bare `msub` and two independently mismatched pairs remain red | yes |
| `audit-deferral` (R15) | a `TODO` in a shipped crate; after the vocabulary gap was found, one README line containing all three newly modeled forms --- `stub`, `placeholder`, and `unfinished work` --- was reported three times at its exact line. The same run also exposed a benign plural in the census report, which was rewritten rather than exempted; the restored tree is clean | yes |
| the former R15 suspension-exemption mechanism | while the bounded phase row existed, a `TODO` in its admitted log was not reported, the same marker in `README.md` was reported, and an `admits` path under `crates/` was rejected by the model's own check before any gate ran. The phase row is now removed, so the log is scanned like every other document | yes |
| the honesty meta-gate (R4) | an ID with no test | yes |
| the differential comparator | a one-element difference | yes |
| the ulp metric | `+0.0` against `-0.0`, and a known one-ulp pair | yes |
| the scaling fit | a known exponent, and too few points | yes |
| the NumPy digest check | a published SHA-256 vector | yes |
| `just features` | `AddressOutcome::label` for `.address` --- the defect `kappa` actually shipped, unbuilt by any gate | yes |
| the README examples | a wrong asserted value in a fenced block; they are doctests now | yes |
| `just miri` | the raw face's window made one element too long: passes the native run, Undefined Behaviour under Miri | yes |
| the raw-window tests (`CS-05`) | the same window made one element too short, caught by all three | yes |
| `CU-07` | `-p uor-matmul-kernels` dropped from the job, from the recipe, and from both --- the last is the defect this workspace actually shipped | yes |
| `CK-10` (arena canonicalization) | duplicates not collapsed by `canonicalize`; a sign-masked comparison is *not* reported --- pattern order never makes `-0.0`/`+0.0` adjacent, and the row says so | yes |
| `CK-09` on the arena tier | an off-by-one `index_of` | yes |
| the `CK-08` arena spelling | `Arena` spelled `Grid` in the canonical manifest | yes |
| `CD-14` | rows and column group swapped in the wide-lane gather | yes |
| `CB-09` | the modular lane's `place` written into the wrong limb (`<< 32` for `Mod32`, `<< 64` for `Mod64`), one at a time --- each failed its own family's end-to-end byte comparison and not the other's | yes |
| `CU-08` | the modular table lane admitted under `Saturating` as well as `Wrapping` --- the census half caught the table running where the exact lane must stream | yes |
| `CB-10` | a sign inversion inside the bound-1 build (the `w == 1` and `w == -1` arms swapped, in both row-specialized and any-row forms) --- the parity sweep failed at the first tile, and the census test's byte assertion failed with the negated product. The AVX2 twin is gated by the same parity test on x86 CI. Also: the bound-1 build's `max_bound` raised to admit bound 2 --- the parity test failed on the missing bound-1 declaration, with the selection half's `choose_table` assertions behind it | yes |
| `CK-11` | the `Sign` decode's bit convention inverted (set is `-1`, clear is `+1`) --- both `CK-11` tests failed, the codec-level one on the first exhaustive code and the gemm-level one on the first shape. `CK-09` did *not* fire: the inversion permutes the table, which the enumeration laws are blind to by design --- the cross-spelling byte identity is the gate for the convention, and it held | yes |
| `CK-12` | the `Ternary` decode's digit convention disturbed, twice: the `-1`/`+1` arms swapped, and the dead digit 3 decoded to `+1` --- each failed the codec-level `CK-12` test on an exhaustive code and the gemm-level one on the first shape; the adds-only bound-1 half did *not* fire under either, because its dense reference shares the tier's decode and only the cross-spelling comparison sees the convention. `CK-09` did not fire under the swap either, for the same reason as under `CK-11`'s | yes |
| `CK-15` | `u8::index_stream` planted to answer `Some(U8)` unconditionally, with the power-of-two guard out --- the `CK-14` non-power-of-two pin failed on the spot (a space-6 arena must build its stream), and `CK-09`'s law-3 mask check stands behind it for every tier that would borrow wrongly. The swap the claim most fears --- the `U8` arm handed to the `u16` gather --- does not compile: the two gathers are distinct fn-pointer types, so that defect class is exhausted at the type level, and what remains is pinned lane for lane by the parity walk's `u8` leg (`CB-08`, extended rather than forked) | yes |
| the Gray sign build | two plants, both caught by the differential test: a store at the walk ordinal instead of the binary code index (`out[step * R]` for `out[gray * R]`), and the flipped bit's index one off (`trailing_zeros() + 1`) --- each failed `the_gray_walk_builds_the_sign_table_slot_for_slot` on the first case. The wrong-codebook class --- `Ternary` at bound 1 handed the walk --- is excluded by construction: the bound alone never selects the build, only the codec's `SIGN_BIT_BOOK` does, and the existing bound-1 suite runs both codecs through the wire's `Auto` path with the dense driver as the oracle | yes |
| `CD-23` | a wrong sub-product assembly in the modular level (`M2` written over the `M5` temporary) --- `the_modular_level_returns_the_direct_walks_bytes_cd_23` failed on the first shape against the direct packed modular walk. The seven-product count is asserted from the level's own increments, not restated, and the decline paths (odd extent, non-wrapping encode, starved offer) are pinned against the direct walk's bytes rather than asserted away | yes |
| `CD-17` | numeric `==` as the float rows' identity (both the element verdict and the contiguous-run comparison) --- all three `CD-17` tests failed: the unit pass counted five rows where bit identity sees two (NaN rows refuse to collapse under `==`), the bit-witness unit test counted five symbols for four, and the end-to-end census left the per-distinct-row closed form | yes |
| `CG-11`, a dropped family | the `table` family classified as `other` in the census --- the conformance test failed on `no table sequence in the x86-64 artifact` | yes |
| `CG-11`, a tool that is not llvm-mca | `LLVM_MCA` pointed at `/usr/bin/false` and at a path that does not exist --- the subcommand failed nonzero, naming the tool it was told to run. Also an impostor that prints the summary counters but no pressure table: the parser failed with `no positive resource pressure` rather than reporting a bottleneck it invented | yes |
| `CG-13` | the materialization built each family's cached list without its first entry (`skip(1)` on the full walk) --- the parity test failed on `i8 tile: the cached list differs from the full walk`, the first family's list comparison | yes |
| `CD-18` (and the arena decode generally) | an off-by-one index in the arena's `decode_element`. The first planting failed only `CK-10` --- `CD-14`, `CD-17`, and `CD-18` all passed, because the harness's reference decoded the operand *through the codec under test*, so a wrong decode shifted both sides of the comparison. The reference now reads the codebook directly with the modulo written out, and the same plant fails `CD-14`, `CD-17`, and `CD-18` on the first shape. `CK-14`'s cross-width test does *not* fire under it: the two widths share the one decode, so a shift shifts both --- that test is consistency between spellings, and the absolute pin is the direct-table reference plus `CK-10`'s fixed positions | yes |
| `CG-14`'s timed-region assertion | one code corrupted after the reference operand was built from the stream --- the byte-identity assertion fired on the first shape (`sym walk 1024x1024x1: the symbol run must give the dense driver's bytes`). Planted one line earlier, before the reference was built, it passed: the reference shares the stream it is derived from, and the row records where the assertion's teeth are | yes |
| `CD-19` | a one-exponent error in the placement scale (`base_a + base_b + 1` handed to the `Scaled` epilogue) --- the byte-equality test failed on the first bridged shape. And the two panels' scale bases swapped in the reification (`A` scaled to `base_b`, `B` to `base_a`) --- failed at the first asymmetric-span case; the one-exponent fill is blind to the swap because its two bases are equal, and the row says so, which is why the sweep carries asymmetric spans. The default driver's auto-selection, twice: `bridge_possible` planted to decline at every type --- the test failed on the type pin itself; and the offer question planted out at the call site --- the byte assertions *passed* (the scalar lanes give the same bytes, which is exactly the vacuity the row exists against) and the side-effect pin failed instead: the offer held packed codes where the table lane would have left the reified operands. The lane-depth term is pinned the same way, as the table's own capacity arithmetic (`lane_depth(2^27) == 511`) plus a depth-past-the-lane call whose offer must not hold the reification. The public full-workspace test now runs the same product from NaN and negative-infinity cache poison plus distinct compatibility-buffer patterns, retains every backing address, and compares every output byte; planting a skipped first cache write makes the two poison runs disagree. It deliberately asserts no post-call workspace value: residue is unspecified and cannot become a second public result | yes |
| `CD-20` | a one-exponent error in `Scaled64::place_scaled` and a lane over-running its derived capacity both failed the exact worst-case corpus. Replacing the f32 q producer with an IEEE round trip, severing its projected cell from `Scaled64::mac`, replacing the signed-octet lookup with multiplication, or pricing that build as multiplying all failed the contextual composition audit. The total-carrier and source-local scheduling plants are recorded under `CD-32`; the live block-one f32 table executes every finite extent and Complete union through q cells, occupied-extent contraction, singleton tags, and scalar fracture. A categorical f64 capacity override and removal of the downstream block-two codec witness also failed; the live f64 block-one and block-two differentials execute the complete Atlas table, preserve dense bytes, and report zero multiply/dense-call census | yes |
| `CG-16`'s census and clock harness | the current gate fixes 32 calibration identities (28 structural keys plus four value-twin rows), 12 B1 holdouts (11 keys), and B3/B5 controls. Bareiss elimination over `BigInt` gives exact ranks 14 and 6 and verifies every coordinate is essential; aliasing the final table coordinate to its neighbour lowers rank. Source plants replace the full-length ternary hash with a legacy word mixer, reduce its seed before the one terminal modulus, mask an EntrySet probe, sever scalar fracture, leak a diagnostic value into the selector, change `<` to `<=`, reverse the adversarial twin envelope, remove either byte guard, put verification inside the timer, omit paired raw rows, and bypass live reconciliation; every mutated audit is required to fail before the restored harness runs. The nonignored C07/B1 and H13/B3 gate checks complete bytes and replayed Census counts against real public calls. The post-native release run emitted all 828 labelled route samples and passed its guards. H01 and H02 then supplied the candidate's own falsifier: their pre-admission `StructuralWork` and asserted candidate route were equal, but their held-out paired table/decline intervals were `0.1792 +/- 0.0318` and `3.9968 +/- 1.7006`. Thus the evidence rejected the candidate without turning a host clock into an asserted threshold | yes |
| `CB-12` | a field overlap one bit wide (the field-1 spread shift planted as 12 for 13, so two fields share bit 20) and a halved compensation term (`<< 13` for `<< 14` in the extraction's `16384 * d`), one at a time --- each failed the parity test on the first packed depth under the wasmtime run (`SWAR broadcast disagrees at kc=1`). The tile-contract half records a real bring-up defect: the sweep's first draft modelled the tile as accumulate-into and the kernel's first draft accumulated into `acc`, where every kernel's contract is overwrite --- the timed-region assertion fired on the second rep at 2x the one-call sum, and the explicit garbage-filled-tile assert now covers the kernel side, which the zeroed-`acc` parity loops cannot see | yes |
| `CD-21` | a wrong sub-product assembly (`M2` and `M5` swapped in the combination) --- five of the six `CD-21` unit tests failed, the hand-computed `2x2` first; only the plan test passed, which is about level counting, not values. And the offer-decline rule, planted in layers: the per-product decline alone did *not* fire, because the plan's offer rule stops a starved offer upstream (the plan test asserts exactly that, and fired when the plan's rule was planted out); with the top-level guard out as well, the offer-ladder byte test failed at the first starved offer --- a `split_at_mut` out of range, which is the corruption a safe language can write | yes |
| `CD-22` | the facade pointed back at the reference (the state the claim exists against): the byte assertions *passed* --- the reference's bytes are the same bytes, which is exactly the vacuity the row exists against --- and the sentinel-offer pin failed instead (`the suggested offer was never written`), because the packed traversal packs its panels into the offer and the reference never touches it. And the census planted silent (`RouteCensus::routed` with an empty body): both census-reading tests failed, the kernel-route one on `must run a table kernel, got None`, the zero-scratch one on the same absence --- a witness that records nothing fails as loudly as a wrong route | yes |
| `CG-12`'s harness, twice | the first draft's "exact cubic" baseline was the modular lane (`EncodeMode::Wrapping` selects it), so the recursion was being compared against a lane it does not factorize --- caught when the exact lane's rate was measured directly at 18 Gmac/s against the column's 26; the exact baseline now runs under `Saturating`, which declines the modular arm. And the first draft's base case ran the per-tile streaming path at deep levels: the plan reserved `suggested_accumulators`, which answers zero whenever `k <= KC`, but the grown bounds make the base case's *lane* shallower than its `k` --- found by an overhead that scaled with `n^3` (L=3 at 2048 read 8.2 Gmac/s where the products alone price at 16), fixed by offering the output block of accumulators unconditionally | yes |
| `CG-18` | a multiply charged per gather read (`ledger.multiplied(computed * depth * rows)` beside the gather's `read`) --- the test failed on the spot (`the only multiplies are the build's (\`code_space * block * rows\` per slot)`), because the running table's multiply count is a closed form and one more is visible exactly. The selection half was falsified the same way the derivation already is: the boundary is recomputed from the host's own declarations, so a hardcoded break-even fails on the ISA it was not derived for --- that failure is the reason the test carries no number. And a real bring-up defect, caught by `CB-05`'s wasm leg rather than by planting: the distinct-column stream built its base-256 digits as `j / space.pow(p)`, and `pow` overflows a 32-bit `usize` at `p = 4`, reading as a divide by zero under wasmtime; the digits are now taken by repeated division | yes |
| `CB-11` (the Cortex-M executor) | the portable tile's row index written `i ^ 1` in `isa/portable.rs` --- the thumbv6m executor printed `CB-11: FAIL` with the assert's location and qemu exited 1, so `just cortex-m-run` failed at the first family, before any PASS marker | yes |
| `CK-16` | `TropAcc::combine`'s `max` replaced by a saturating sum --- four tests failed, and the *first* assertion to go was the identity rather than idempotence (`Tropical<i8>: the identity must be `Accumulator::ZERO`, or padding is not exact`), because `-inf + a` is not `a`. `CD-26`'s two gauge tests failed with it, since a section defined by a maximum is not defined by a sum. Three rows did *not* fire and the row records why: `CT-08` is about absorption, `CK-17` about the *value* of the semiring zero, and `CA-04` about the accumulator's *width* --- three claims genuinely about something else, which is the control | yes |
| `CD-25` | the D-6 tie-break inverted in mechanism A alone (`Lex::combine`'s `other.at < self.at` written `>`) --- `both_mechanisms_give_the_same_witness_cd_25` failed at `shape 2x5x3, panel 0` with witness `[2, 2, 2, 3, 3, 3]` against `[2, 2, 2, 0, 0, 0]`, the degeneracy row failed with `Lexicographic: every term attains the maximum, left: 4, right: 0`, and the `gemm_selected` doctest failed | yes |
| `CD-24`, and the reason it has two halves | the same inversion applied to **both** mechanisms (A's comparison inverted and B's `break` removed, so its scan keeps the last match) --- `CD-25` **passed**, because two mechanisms that are wrong together still agree, and that vacuity is exactly what this row exists against. Under the one-mechanism plant above, `CD-24`'s partition sweep *also* passed: "largest index" is a total order too, so invariance under partition survives an inverted convention. `CD-24`'s test therefore carries a second half that pins the convention on a forced tie at both mechanisms, and that half failed with `Lexicographic: D-6 declares the smallest index, not the largest`. The pair is the two-sided evidence --- `CD-25` catches one mechanism drifting, `CD-24` catches both drifting together, and neither alone suffices | yes |
| `CM-02`, the class-completeness half | an ID registered under a prefix `codegen::CLASSES` does not render (`CZ-01`) --- `check-model` failed with ``CZ-01: `CONFORMANCE.md` renders no class this ID belongs to, so the row would be gated everywhere and published nowhere``. The control is the pre-change behaviour, also exercised: with the check bypassed and `CZ-01` still registered, `check-model` printed `the generated consts equal the model (CM-01)` and exited 0 while `CZ-01` appeared **zero** times in `CONFORMANCE.md` --- the rendered bytes and the committed bytes agreed on the absence, which is precisely why no byte comparison could catch it | yes |
| the `refuted_by` requirement | `refuted_by` emptied on `CD-01`, then on `AUTH-TF1-D8`: `a claim with no refutation condition is not falsifiable; every row states what would refute it`, once per row. Removing the key entirely fails earlier, in the parser --- ``missing field `refuted_by` `` --- because the field is required rather than an `Option`, so a row cannot be added without one | yes |
| `CB-13`, on every ISA | six plants, one at a time, each run on the ISA it names and not merely compiled for it: `_mm256_adds_epi16` written `_mm256_add_epi16` (`CB-13: Avx2 disagrees at kc=1`, the masked lanes reading `0` and `32767` where `-32768` was due); `vqaddq_s16` written `vaddq_s16`, under `qemu-aarch64` (`CB-13: Neon disagrees at kc=1`); `i16x8_add_sat` written `i16x8_add`, under `wasmtime` (a wasm `unreachable` trap, exit 134); the portable tile's identity written `0` for `TROP_ZERO` (`CB-13: Portable disagrees at kc=0`, and the empty reduction read `Some(0)` for `None`); `TROP_I16_MAX_BOUND` widened by one, which fails at **build** time --- ``evaluation panicked: an absorbed lane must lose to every finite lane, or `max` is not `⊕` ``; and `unpack`'s threshold loosened from `<` to `<=` (`the lowest finite result is finite`). The saturating add is the whole correctness argument, and five of the six plants are ways of losing it | yes |
| `CU-10` | the scope and the scanner, separately. A tropical body containing one `imull` between a `vpaddsw` and a `vpmaxsw` is reported; the same body without it is not --- so a scanner that recognised nothing would fail the second half. And a tropical entry point renamed to drop the `trop_i16` fragment fails the source half, which is what keeps `audit-disassembly` from reading an empty set: with no such symbol the gate refuses rather than passing, ``CU-10 found no symbol containing `trop_i16` `` | yes |
| `CS-10` | three plants: selection made to probe the operand (`C::index_of(codes[0]) != 0` added to the addressability test) --- failed at `n = 683`, the all-zero-code operand selecting `Blocked` where every other value pattern selected `Tabulated`; the orientation read from the wrong axis; and the block ignored, so `Addressing::of` reported a run at `MAX_BLOCK == 1`. The first is the one the row exists against: a selector that reads data is invisible until the data changes | yes |
| `CD-28` | three plants: a knob other than the mode moving the bytes (`c[0]` zeroed at a panel offer of 1) --- failed at `OutputMajor on Auto, a panel offer of 1, tiled 5x6, at Nearest`; the mode made inert on the ring side; and the mode made inert on the tropical side, by widening the output so no accumulated max could leave its range. The last is why the row is run at *both* element types: a version tested only at `i8` would have passed while saying nothing about the quantifier it exists to state | yes |
| `CM-02`, the reachability half | `#[ignore]` on `the_narrow_tile_path_agrees_with_dot_wide_cd_09`, whose ID is registered: `CD-09 is registered and every test named for it is unreachable, so the claim runs nowhere. A test that is compiled out, or skipped, is not evidence --- it is a name.` The gate names the redirection it looked for and did not find: ``no Justfile recipe runs `cargo test -p uor-matmul-core --lib -- --ignored` `` | yes |
| `CM-02`, the feature half | `#[cfg(feature = "slow-tests")]` on the same test, a feature its crate does not declare --- ``is false for `uor-matmul-core` at its default features (none), and no Justfile recipe runs `cargo test` for that package with the feature on, so nothing compiles it``. This half found a real one: `CX-08`'s harness sits behind `ref-gemm`, which is not default, and no recipe ran it. Compiling a test is not running it, and `just features` now does | yes |
| `CM-02`, the coverage half | a workspace member `tools/prover` added to the root manifest, outside the scan roots --- three tests failed with ``the workspace member `tools/prover` lives at tools/prover, which no scan root covers``. Without it, a test that moved to a new crate would satisfy the gate while running nowhere | yes |
| the API lock's removal and rename teeth | `pub fn dot_ref` made private, and separately renamed to `dot_reference`; each exact-surface comparison reported one removed declaration and named `uor_matmul_core::dot::dot_ref [fn]`. The addition tooth is planted separately above. The restored control reports 1240 declarations exactly equal in both directions | yes |
| the API constraint, against the pre-refactor tree | every line newly visible after repairing the scanner was checked in `HEAD`: the two float convenience entries, all lookup specifications and their root re-export, and `tile_fits` already existed there. The baseline diff is therefore scanner coverage, not an accepted API addition. `Factorization` is likewise byte-identical to `HEAD`; a downstream exhaustive `match f { Exact, Modular }` that failed against the abandoned mid-refactor variant compiles against the live tree | yes |
| `R11`, the provenance cross-check | `ndarray_i32_is_byte_identical_cx_01` renamed --- `CX-01: crates/uor-matmul-validate/tests/cross_library.rs declares no fn ndarray_i32_is_byte_identical_cx_01; the provenance record points at nothing`. This is the check that did not exist: `verify-oracles` substring-matched the oracle *ID* and never read the `harness` field, and four of the eleven recorded harness names had rotted to functions that do not exist | yes |
| `CX-11` | a byte appended to `oracles/tropical/13x17x19.expected.bin` --- the harness reported the artifact `has drifted from its recorded digest`, with both SHA-256s. Separately, the generator's own mask phase moved: the CI regeneration step exited 1 on `git diff --exit-code` over eleven `.bin` files. The two are deliberately complementary --- regeneration overwrites a corrupted artifact, so only the digest check sees that, and only regeneration sees a generator that drifted | yes |
| `CS-12` | the two defects an adversarial review found alive, one at a time. `ShiftExact::shift_exact` planted to discard its scalar (`(Some(v), Some(_s)) => Self::of(v)`): the row failed at `alpha = -40`. `AbsorbPrior::of_prior` planted to map the semiring zero in `C` to a finite `0`: the row failed with ``-inf` in C is absorbed, not floored at zero`. **Both plants previously survived `cargo test --workspace` with zero failures** --- `MaxPlus` was only ever constructed as `OVERWRITE`, whose `alpha` is the multiplicative identity and whose `beta` is the semiring zero, so `⊗` was evaluated only at an identity and `of_prior` never at all. This row exists because the review found that, and it is the tropical sibling `CS-05` had had all along | yes |
| `CS-13` | a product sum at the declared reachable depth, scaled by `i64::MAX` in the float `Linear` epilogue while `Complete` still discarded carry above its low limbs. The exact positive expression encoded as `-inf`; the test reported ``a reachable positive terminal overflow lost its sign before encode`` with `left: -inf, right: inf`. The same test first cancels `i64::MIN * 0.5 + 2^62 * 1` under every encode mode and passed that control even before the repair: wrapping is a ring homomorphism, so the adversarial hypothesis that fixed-width wrapping by itself loses a later representable cancellation was refuted. The live defect was loss of the final expression's high sign and magnitude when it remains outside the low carrier. The extension-word repair then planted its initial three-canonical-sentinel representation against the pre-existing public `Complete`: `set_nan`/`set_infinity` erased low limbs, opposite infinities collapsed to the NaN state, derived `Debug` exposed `state`, and `Eq`/`Hash` lost the seven former flag combinations. `complete_tail_preserves_former_flag_observations_cs_13` now walks all seven nonempty unions and fails each of those plants while checking raw limbs, sign/magnitude, former Debug field shape, former Hash field stream, equality distinctions, scale, combine, and opposite-infinity NaN behavior; the model mutation from `2^3 - 1` back to three states fails independently | yes |
| `CD-29`, the structural half | a semiring branch planted inside the traversal --- `if <AccOf<E> as Accumulator>::BITS <= 66 { ...a second reduction... }`, which is exactly a test of which algebra the caller is in, since the ring accumulator at `i8` is 79 bits and the tropical one 10. The four-number value check **passed** it, and so did all 80 tests in the crate: a driver that dispatches on the semiring computes the same numbers by a second route. The row now reads its own source and fails on any of `BITS`, `Trop`, `IntegerElement`, `FloatElement`, `TypeId`, `type_name` appearing in `gemm`'s body, and asserts the two operations it *does* name are present so the absence is parametricity rather than an empty function | yes |
| `CD-04`'s tropical half, against vacuity | the fixture could not produce a masked *output* cell at all: with `K = 97` and a mask every eleventh element, some term of every cell is always live, so the assertion that claimed to witness A-6 reaching the answer was `any(is_finite)` --- trivially true and about nothing. Row 0 of `A` is now masked entirely, and both halves are asserted: `reference[..7]` all at the semiring zero, `reference[7..]` all finite. Inverting either assertion fails the test | yes |
| `audit-deferral`'s coverage against its own claim | the gate said "anywhere in the workspace" while walking `crates/`, `xtask/`, `fuzz/` and eight named documents. A `TODO ... later version` in `docs/conceptual-model/README.md` --- a file this change edits --- and a `# TODO ... for now` in `.github/workflows/ci.yml` both passed, and `validate` printed `every gate passed`. The walk now covers `docs/`, `model/`, `features/`, `oracles/`, `.github/`, every top-level `.md` and `.toml` found rather than listed, and the `Justfile`; and the extension filter admits `.yml`, `.yaml`, `.feature`, `.py` and `.json`, without which the `ci.yml` plant still passed after the directory was added. Both plants now fail, naming file and line; the control is clean | yes |
| `CD-24`'s corpus, against vacuity | `draw_tropical` widened from its deliberately narrow range to the whole of `i8` --- `3x7x11: the narrow draw tied nothing`. Measured over the corpus, the narrow draw ties 4586 of 17656 cells and the ring corpus's own fill ties 52. A tie-break gate over a wide random sweep would have read as evidence while testing nothing | yes |

`audit-disassembly` deliberately does *not* catch a float add the optimizer
removed, because such an add is not in the shipped kernel. The gate reports the
binary rather than the source, which is the only thing worth reporting.

## What the suite does not establish

The upstream formalization's theorems. `CL-MM01` .. `CL-MM04` are cited, never
re-derived, and a `CX-*` result is evidence that the kernels realize the
identity --- not a proof of it. `model/authorities.toml` records what is cited
and from where; the meta-gate fails the build if any document blurs the two
registers.
